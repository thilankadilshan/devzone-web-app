import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Setting this to false completely nukes the indicator in the newest Next.js versions
  devIndicators: false,
};

export default nextConfig;
